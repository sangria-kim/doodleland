import 'package:flutter/material.dart';

class MotionSelector extends StatelessWidget {
  const MotionSelector({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
